#!/bin/bash

curl -XPUT 'localhost:9200/_river/mydb/_meta' -d "$(cat <<JSCFG
{
	"type" : "mysql",
	"streamer" : {
		"host" : "localhost",
		"port" : 8080,
		"verify_https" : false
	},
	"mysql" : {
		"script" : "ctx.doc.test111 = 'test'; ctx.id = ctx.doc.tag_id;"
	}
}
JSCFG
)"

